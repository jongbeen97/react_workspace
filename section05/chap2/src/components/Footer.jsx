const Footer = () => {
  return (
    <footer>
      <p>Footer</p>
    </footer>
  );
};

// 컴포넌트를 내보내기위해 export default 키워드 사용
// ES 모듈 시스템에 의해 기본값으로 내보내지므로 app.jsx에서 import 가능
export default Footer;
